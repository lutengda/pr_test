name: Mirror and run GitLab CI on PR

on:
  workflow_dispatch:
  issue_comment:
    types: created

jobs:
  print-message:
    runs-on: ubuntu-latest
    steps:
    - name: Print a message
      run: echo "Hello, world!1"
